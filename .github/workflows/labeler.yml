name: Label Pull Request
on:
  pull_request:
    types:
      - opened

jobs:
  label:
    runs-on: ubuntu-latest
    steps:
      - name: Add label to pull request
        run: gh pr edit $LINK --add-label $LABEL
        env:
          GITHUB_TOKEN: ${{ secrets.API_TOKEN }}
          LINK: ${{ github.event.pull_request.html_url }}
          LABEL: '📦 entrega'
